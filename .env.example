OPENAI_API_KEY=your_openai_api_key_here

# Frame capture intervals (in milliseconds)
# Idle mode: when user is not speaking (default: 6000ms = 1 frame every 6 seconds)
IDLE_FRAME_INTERVAL_MS=6000
# Speak mode: when user is speaking (default: 500ms = 2 frames per second)
SPEAK_FRAME_INTERVAL_MS=500
